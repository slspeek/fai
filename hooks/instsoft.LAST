#! /bin/bash

if [ $FAI_ACTION != "install" ] && [ $FAI_ACTION != "dirinstall" ]; then
   exit 0
fi

if ifclass EXTREPO; then
	if [ X$verbose = X1 ]; then
		$ROOTCMD apt-get update
	else
		$ROOTCMD apt-get update > /dev/null
	fi  
fi
